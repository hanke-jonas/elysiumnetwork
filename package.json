{
  "name": "elysium-network-website",
  "version": "1.0.0",
  "private": true,
  "description": "Elysium+ Network — static site (Eleventy) on Cloudflare Pages",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "deploy": "npm run build && wrangler pages deploy _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "autoprefixer": "^10.5.4",
    "postcss": "^8.5.23",
    "tailwindcss": "^3.4.19",
    "wrangler": "^3.78.0"
  }
}
