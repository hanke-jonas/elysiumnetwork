{
  "name": "elysium-network-website",
  "version": "1.0.0",
  "private": true,
  "description": "Elysium+ Network — static site (Eleventy) + Cloudflare Pages Functions/D1 backend",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "deploy": "npm run build && wrangler pages deploy _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "wrangler": "^3.78.0"
  }
}
